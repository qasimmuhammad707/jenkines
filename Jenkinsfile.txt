pipeline {
  agent any

  stages {
    stage('Checkout') {
      steps {
        git branch: 'main',
            url: 'https://github.com/qasimmuhammad707/jenkines.git'
      }
    }

    stage('Deploy') {
      steps {
        sh 'sudo cp -r * /var/www/html/'
      }
    }
  }
}